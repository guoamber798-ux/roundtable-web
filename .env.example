# 部署到公网时：在平台环境变量里设置这一项即可，访客无需自备 Key
OPENAI_API_KEY=sk-your-key-here

# 默认模型（推荐 mini 省钱）
DEFAULT_MODEL=gpt-4o-mini

# 每 IP 每小时请求上限，防滥用（0=不限）
RATE_LIMIT_PER_HOUR=10

# 仅当未设置服务端 Key 时，是否允许访客自带 Key
ALLOW_CLIENT_API_KEY=false
